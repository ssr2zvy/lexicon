[package]
name = "lexicon-core"
version = "0.1.2"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
base64 = "0.22.1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"

[dev-dependencies]
trybuild = "1.0"
