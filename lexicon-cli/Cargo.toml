[package]
name = "lexicon-cli"
version = "0.1.0-LOCALSNAPSHOT"
edition = "2024"

[build-dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"

[dependencies]
clap = { version = "4", features = ["derive"] }